# Validation artifacts

`v061_dry_run_report_v0.4.json` records the offline canonical-workbook validation performed while preparing development environment v0.4.

It confirms the exact v0.6.1 workbook checksum and current workbook invariants, but it is **not** evidence that PostgreSQL/PostGIS migrations have executed successfully. The first live Docker/PostGIS run remains pending.
